version: 1.0
runtime: python311

run:
  runtime-version: 3.11
  pre-run:
    - python3 -m pip install --no-cache-dir -r requirements.txt
  command: python3 -m streamlit run main.py --server.port=8080 --server.address=0.0.0.0
  network:
    port: 8080
